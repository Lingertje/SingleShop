name: Tests

on:
  push:
    branches:
      - main
      - feature/*

jobs:
  test:
    name: Run tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: |
          yarn
          yarn test


